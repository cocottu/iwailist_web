# PWA セットアップガイド

このドキュメントでは、PWA（Progressive Web App）機能の詳細な設定方法を説明します。

## 📋 目次

1. [PWA機能の概要](#pwa機能の概要)
2. [Service Worker設定](#service-worker設定)
3. [アイコン生成](#アイコン生成)
4. [カスタムフックの使用](#カスタムフックの使用)
5. [開発とデバッグ](#開発とデバッグ)
6. [デプロイ前チェックリスト](#デプロイ前チェックリスト)

## PWA機能の概要

### 実装済みの機能

- ✅ **Service Worker**: アプリケーションシェルのキャッシュ
- ✅ **オフライン動作**: ネットワークなしでも利用可能
- ✅ **インストール可能**: ホーム画面に追加可能
- ✅ **自動更新**: 新バージョンの自動検知と更新
- ✅ **オフライン検知**: ネットワーク状態の自動検知
- ✅ **キャッシュ戦略**: 効率的なリソース管理

### アーキテクチャ

```
┌─────────────────────────────────────────┐
│         ブラウザ（クライアント）          │
├─────────────────────────────────────────┤
│  React Application                      │
│  ├── OfflineIndicator                   │
│  ├── PWAInstallPrompt                   │
│  └── UpdatePrompt                       │
├─────────────────────────────────────────┤
│  Custom Hooks                           │
│  ├── useOnlineStatus()                  │
│  ├── usePWAInstall()                    │
│  └── useSWUpdate()                      │
├─────────────────────────────────────────┤
│  Service Worker (Workbox)               │
│  ├── Cache Strategy                     │
│  ├── Background Sync                    │
│  └── Offline Fallback                   │
├─────────────────────────────────────────┤
│  IndexedDB                              │
│  └── Local Data Storage                 │
└─────────────────────────────────────────┘
```

## Service Worker設定

### キャッシュ戦略

`vite.config.ts`で以下のキャッシュ戦略を設定しています：

| リソースタイプ | 戦略 | キャッシュ期間 | 説明 |
|---------------|------|---------------|------|
| フォント（Google Fonts） | CacheFirst | 1年 | 静的リソースを優先的にキャッシュ |
| 画像（PNG/JPG/SVG） | CacheFirst | 30日 | 画像を長期キャッシュ |
| API リクエスト | NetworkFirst | 5分 | ネットワークを優先、失敗時キャッシュ |
| アプリケーション本体 | AutoUpdate | - | 新バージョンを自動検知 |

### 設定のカスタマイズ

`vite.config.ts`を編集してキャッシュ戦略を変更できます：

```typescript
workbox: {
  runtimeCaching: [
    {
      urlPattern: /^https:\/\/your-api\.com\/.*/i,
      handler: 'NetworkFirst',
      options: {
        cacheName: 'api-cache',
        networkTimeoutSeconds: 10,
        expiration: {
          maxEntries: 50,
          maxAgeSeconds: 60 * 5 // 5分
        }
      }
    }
  ]
}
```

## アイコン生成

### 必要なアイコン

PWAには以下のアイコンが必要です：

```
public/
├── pwa-192x192.png              (192x192px, 通常アイコン)
├── pwa-512x512.png              (512x512px, 通常アイコン)
├── pwa-maskable-192x192.png     (192x192px, マスカブル)
└── pwa-maskable-512x512.png     (512x512px, マスカブル)
```

### 生成方法

#### 方法1: オンラインツール（推奨）

1. [PWA Builder Image Generator](https://www.pwabuilder.com/imageGenerator) にアクセス
2. `public/pwa-icon-template.svg` をアップロード
3. 生成されたアイコンをダウンロード
4. `public/` ディレクトリに配置

#### 方法2: RealFaviconGenerator

1. [RealFaviconGenerator](https://realfavicongenerator.net/) にアクセス
2. SVGファイルをアップロード
3. PWAオプションを選択
4. 生成されたアイコンをダウンロード

#### 方法3: 手動作成

デザインツール（Figma、Illustrator等）で以下の仕様に従って作成：

**通常アイコン:**
- サイズ: 192x192px および 512x512px
- フォーマット: PNG
- 背景: 透過または単色
- 余白: 推奨なし

**マスカブルアイコン:**
- サイズ: 192x192px および 512x512px
- フォーマット: PNG
- セーフゾーン: 中央80%にアイコンを配置
- 背景: 必須（透過不可）
- 余白: 上下左右20%

### アイコンのテスト

1. [Maskable.app](https://maskable.app/) でマスカブルアイコンをテスト
2. Chrome DevToolsで確認:
   - DevTools > Application > Manifest
   - アイコンが正しく表示されるか確認

## カスタムフックの使用

### useOnlineStatus()

オンライン/オフライン状態を検知：

```typescript
import { useOnlineStatus } from '@/hooks';

function MyComponent() {
  const isOnline = useOnlineStatus();
  
  return (
    <div>
      {isOnline ? 'オンライン' : 'オフライン'}
    </div>
  );
}
```

### usePWAInstall()

PWAインストール機能を提供：

```typescript
import { usePWAInstall } from '@/hooks';

function InstallButton() {
  const { isInstallable, promptInstall } = usePWAInstall();
  
  if (!isInstallable) return null;
  
  return (
    <button onClick={promptInstall}>
      アプリをインストール
    </button>
  );
}
```

### useSWUpdate()

Service Worker更新機能：

```typescript
import { useSWUpdate } from '@/hooks';

function UpdateNotification() {
  const { needRefresh, updateServiceWorker } = useSWUpdate();
  
  if (!needRefresh) return null;
  
  return (
    <div>
      <p>新しいバージョンが利用可能です</p>
      <button onClick={() => updateServiceWorker(true)}>
        更新
      </button>
    </div>
  );
}
```

## 開発とデバッグ

### 開発モードでのPWA

開発サーバーでPWA機能をテスト：

```bash
npm run dev
```

`vite.config.ts`で`devOptions.enabled: true`が設定されているため、開発モードでもService Workerが動作します。

### Chrome DevToolsでのデバッグ

1. **Application タブ**
   - Service Workers: 登録状態を確認
   - Manifest: マニフェストファイルを確認
   - Storage: キャッシュとIndexedDBを確認

2. **Network タブ**
   - Service Workerからのレスポンスを確認
   - キャッシュヒット率を確認

3. **Lighthouse**
   - PWAスコアを測定
   - 改善提案を確認

### オフライン動作のテスト

```bash
# プロダクションビルドでテスト
npm run build
npm run preview
```

Chrome DevToolsで：
1. Network タブを開く
2. "Offline" を選択
3. ページをリロード
4. オフラインでも動作することを確認

### Service Workerのリセット

問題がある場合、Service Workerをリセット：

1. Chrome DevTools > Application > Service Workers
2. "Unregister" をクリック
3. Application > Clear storage
4. "Clear site data" をクリック
5. ページをリロード

## デプロイ前チェックリスト

### 必須項目

- [ ] PWAアイコンを生成して配置
  - [ ] `public/pwa-192x192.png`
  - [ ] `public/pwa-512x512.png`
  - [ ] `public/pwa-maskable-192x192.png`
  - [ ] `public/pwa-maskable-512x512.png`

- [ ] `manifest.json` の設定確認
  - [ ] アプリ名が正しい
  - [ ] テーマカラーが設定されている
  - [ ] アイコンのパスが正しい

- [ ] Service Worker動作確認
  - [ ] オフラインでページが表示される
  - [ ] キャッシュが機能している
  - [ ] 更新通知が表示される

- [ ] HTTPSで配信
  - [ ] 本番環境はHTTPS必須
  - [ ] localhost以外はHTTPS必要

### 推奨項目

- [ ] Lighthouse PWAスコア
  - [ ] スコア90以上を目指す
  - [ ] 全ての監査項目を確認

- [ ] クロスブラウザテスト
  - [ ] Chrome（Android/Desktop）
  - [ ] Safari（iOS/macOS）
  - [ ] Firefox
  - [ ] Edge

- [ ] パフォーマンス最適化
  - [ ] 初回ロード時間 < 3秒
  - [ ] キャッシュヒット率 > 80%
  - [ ] バンドルサイズ最小化

### テストコマンド

```bash
# ビルド前チェック
npm run lint
npm run test
npm run test:e2e

# プロダクションビルド
npm run build

# ビルド後の確認
npm run preview
```

## トラブルシューティング

### よくある問題

**Q: Service Workerが登録されない**
- HTTPSまたはlocalhostでアクセスしているか確認
- ブラウザのService Workerサポートを確認
- コンソールエラーを確認

**Q: オフラインで動作しない**
- 一度オンラインでページを読み込む必要がある
- Service Workerが "activated" 状態か確認
- キャッシュにリソースが保存されているか確認

**Q: 更新が反映されない**
- ハードリロード（Ctrl+Shift+R / Cmd+Shift+R）
- Service Workerを手動でアンレジスター
- ブラウザキャッシュをクリア

**Q: インストールボタンが表示されない**
- HTTPSでアクセスしているか確認
- マニフェストファイルが正しいか確認
- 既にインストール済みでないか確認

## 参考リンク

- [PWA ドキュメント (MDN)](https://developer.mozilla.org/ja/docs/Web/Progressive_web_apps)
- [Workbox ドキュメント](https://developers.google.com/web/tools/workbox)
- [Vite PWA Plugin](https://vite-pwa-org.netlify.app/)
- [web.dev PWA Guide](https://web.dev/progressive-web-apps/)
